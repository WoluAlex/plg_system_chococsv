{
  "config": {
    "platform": {
      "php": "8.3.2"
    },
    "github-protocols": [
      "https"
    ],
    "optimize-autoloader": true,
    "allow-plugins": true,
    "sort-packages": true
  },
  "prefer-stable": true,
  "require": {
    "php": "^8.1",
    "ext-curl": "*",
    "ext-gd": "*",
    "ext-json": "*",
    "ext-mbstring": "*",
    "ext-simplexml": "*"
  },
  "require-dev": {
    "symfony/dotenv": "^7.0",
    "phpunit/phpunit": "^11.0"
  },
  "autoload": {
    "psr-4": {
      "AlexApi\\Plugin\\Console\\Chococsv\\": "src/src/"
    }
  },
  "autoload-dev": {
    "psr-4": {
      "Tests\\": "Tests/"
    }
  },
  "scripts": {
    "gen": "export current_hash=\"$(php ./tools/deploy-hash.php $PWD 16)\" && { mkdir -p $HOME/Volumes/PROJECTS/build && cd ./src [ ! -f \"$HOME/Volumes/PROJECTS/build/$(basename $(dirname $PWD))_${current_hash}.zip\" ] && zip -9 -r $HOME/Volumes/PROJECTS/build/$(basename $(dirname $PWD))_${current_hash}.zip . && cd .. ; }"
  }
}
